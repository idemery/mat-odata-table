import { OdataFieldDirective } from './odata-field.directive';

describe('OdataFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new OdataFieldDirective();
    expect(directive).toBeTruthy();
  });
});
